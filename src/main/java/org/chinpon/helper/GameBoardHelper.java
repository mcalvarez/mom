package org.chinpon.helper;

import org.chinpon.model.entities.GameBoard;
import org.chinpon.model.entities.History;

public class GameBoardHelper {
	public static GameBoard createNewBoard(History history) {
		GameBoard board = new GameBoard(history.getGameBoard());
		
		// Add objective cards
		
		// Add rooms
		
		
		return board;
	}
}
